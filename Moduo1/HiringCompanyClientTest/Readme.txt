Directories(namespaces):

	->HiringCompanyClientTest.Command
		-test for each client command

	->HiringCompanyClientTest.Model
		-test for each model that represent enitity in real world

	->HiringCompanyClientTest.ViewModel
		-test for each ViewModel component 